"""사용자에게 그대로 보여줄 오류의 공통 조상.

"ffmpeg 를 찾을 수 없습니다" 같은 것은 사용자가 읽고 조치할 수 있는 안내다. 이것을 프로그램
버그와 같은 취급으로 500 과 트레이스백으로 내보내면, 정작 할 일(설치)이 파묻힌다.

이 갈래로 묶어두면 서버와 백그라운드 작업이 한 곳에서 같은 규칙으로 다룰 수 있다.
버그는 여전히 트레이스백을 남기고, 이쪽만 안내 문구로 나간다.
"""

from __future__ import annotations


class UserError(Exception):
    """읽고 조치할 수 있는 오류. 메시지를 그대로 화면에 띄운다."""
